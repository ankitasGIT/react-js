
## NOTES

1. .env
- If create-react app used then in env prefix should be REACT_APP otehrwise undefined && process.env.name | vite then VITE_ is prefix && import.meta.env.name
- Use .env.sample for reference good practise.
- Always restart the server if env fields are updated
- Use config/conf.js




