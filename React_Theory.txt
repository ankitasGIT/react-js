1. React is a library. 
2. React-DOM : web
3. React-native : Phone


4. npx create-react-app name
npx - node package executor
create-react-app - utility

4. npm run build:

- build folder is created 
- serves all the users
- css, js
- build folder is ready for deploying

5. npm create vite@latest

- 

6. BASICS
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

- react-scripts: Injects js in html (Vite - directly loads js into src)
- React.StrictMode: 
- In vite: always use .jsx | not needed in react basics
- <> add empty frgament to avoid error for return html block in Vite.
- Upper case for component name in both ways.

